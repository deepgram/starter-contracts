{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "QueryParameters",
  "description": "Query parameters for TTS synthesis - all actual Deepgram TTS API parameters",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "model": {
      "type": "string",
      "default": "aura-asteria-en",
      "description": "AI model used to process submitted text (63+ models available)"
    },
    "callback": {
      "type": "string",
      "description": "URL to which callback request will be made"
    },
    "callback_method": {
      "type": "string",
      "enum": [
        "POST",
        "PUT"
      ],
      "default": "POST",
      "description": "HTTP method for callback request"
    },
    "mip_opt_out": {
      "type": "boolean",
      "default": false,
      "description": "Opt out of Deepgram Model Improvement Program"
    },
    "bit_rate": {
      "oneOf": [
        {
          "type": "integer"
        },
        {
          "type": "string"
        }
      ],
      "default": 48000,
      "description": "Audio bitrate in bits per second (default: 48000 bps for mp3)"
    },
    "container": {
      "type": "string",
      "default": "wav",
      "description": "File format wrapper for output audio (default: wav)"
    },
    "encoding": {
      "type": "string",
      "default": "mp3",
      "description": "Expected encoding of audio output (default: mp3)"
    },
    "sample_rate": {
      "type": "integer",
      "default": 24000,
      "description": "Sample rate for output audio in Hz (default: 24000)"
    }
  }
}